---
description: 雅速雅思 (YASU IELTS) 前端设计规范 — 编写任何 UI / 页面 / 组件时必须遵守
alwaysApply: true
---

# 雅速雅思 YASU IELTS · 前端设计规范

为雅速雅思（一家雅思培训机构）编写前端代码时，**严格遵守**以下设计系统。
品牌气质：**编辑式排版 / 朱红 + 墨黑 + 纸白 / 干净克制 / 有速度感**。不要做成普通 SaaS 落地页。

---

## 1. 色彩 Tokens（只用这些颜色，不要自创）

在 `:root` 中定义并全程引用：

```css
:root{
  --ink:    #15151A;  /* 主文字 / 深色区背景 */
  --ink2:   #5b5b62;  /* 次要文字 / 说明文字 */
  --red:    #C8102E;  /* 品牌朱红 — 强调、CTA、速度斜杠、编号 */
  --redsoft:#FFF4F5;  /* 朱红的极浅底色 — 标签底、提示块 */
  --paper:  #FAFAF7;  /* 主背景（纸白，非纯白）*/
  --paper2: #F1F0EC;  /* 次级面板 / 卡片分隔底 */
  --line:   #E4E3DE;  /* 描边、分隔线 */
}
```

- 主背景永远用 `--paper`（纸白），**不要用纯白 `#fff`** 作大面积底色；纯白只用于卡片。
- 朱红是**唯一强调色**，用于 CTA、编号、速度斜杠、关键数字、标签。不要再引入第二个强调色（蓝/绿/紫等）。
- 深色区（如 AI 工具区、页脚）用 `--ink` 做底，文字反白。
- 需要新色阶时用 `oklch()` 在以上色板里推导，**不要凭空造新色**。
- 禁止：大面积渐变背景、彩色玻璃拟态、霓虹色。

---

## 2. 字体 Typography

引入这四款（Google Fonts）：

```html
<link href="https://fonts.googleapis.com/css2?family=Sora:wght@400;500;600;700;800&family=Noto+Sans+SC:wght@400;500;700;900&family=Noto+Serif+SC:wght@600;700;900&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
```

| 用途 | 字体 | 说明 |
|---|---|---|
| 中文标题 / 正文 | `'Noto Sans SC'` | 标题用 900，正文 400/500 |
| 拉丁数字 / 分数 / 英文大标 | `'Sora'` | 分数、Band score、统计数字一律用 Sora 800 |
| 标签 / eyebrow / 元信息 | `'Space Mono'` | 大写、`letter-spacing:.14em~.22em`，多为 `--ink2` 或 `--red` |
| 引言 / 装饰性衬线 | `'Noto Serif SC'` | 仅用于大引号、点缀 |

字体栈统一：`font-family:'Sora','Noto Sans SC',sans-serif;`

**禁止**使用 Inter / Roboto / Arial 等被滥用的字体。

字号下限：正文 ≥13px，移动端点击区 ≥44px。

---

## 3. 标志性视觉语言（务必体现，这是品牌识别度来源）

### a. 速度斜杠 "Speed Marks"
朱红的倾斜平行四边形，成对出现（一粗一细、后者半透明），象征"速度/提分"：
```html
<span style="width:20px;height:76px;background:var(--red);transform:skewX(-16deg);"></span>
<span style="width:12px;height:76px;background:var(--red);opacity:.5;transform:skewX(-16deg);"></span>
```
用在大标题旁、装饰角落。

### b. 编号分区头 Section Header
每个版块用「数字编号 / 中文标题」+ 英文小标的编辑式结构：
```html
<span style="font-family:'Space Mono';font-size:12px;letter-spacing:.22em;color:var(--red);">01 / 智能工具</span>
<span style="font-family:'Space Mono';font-size:12px;letter-spacing:.22em;color:#b9b9b3;">AI TOOLS</span>
<!-- 紧接一个 Noto Sans SC 900 / ~46px 的中文大标题 -->
```

### c. 斜杠分隔的跑马灯 / 元信息
关键词之间用朱红 `/` 分隔：`一对一定制 <span style="color:var(--red)">/</span> 真题课件 ...`

### d. 大号关键数字
分数、统计永远突出：`Sora 800`，朱红或墨黑，配小号中文标签（"目标分""总分"）。

### e. 朱红标签 Pill / Chip
小标签用胶囊：白底 `--line` 描边，或 `--redsoft` 底 + `--red` 字。

---

## 4. 布局 Layout

- 内容最大宽度 `max-width:1180px; margin:0 auto;`，左右 padding 28px。
- 版块上下 padding 约 `96px`，版块之间用 `border-bottom:1px solid var(--line)` 分隔。
- 背景节奏：纸白 → 墨黑（深色强调区）→ 纸白 → 纯白卡片区，交替制造呼吸感；全站深色区最多 1–2 处。
- **多元素排列一律用 flex / grid + `gap`**，不要用 margin 或空白字符撑间距。
- 卡片：白底、`border:1px solid var(--line)`、`border-radius:18~22px`，阴影克制（`0 24px 60px rgba(20,20,25,.10)`）。
- 圆角统一在 10–22px 区间，不要超大圆角气泡感。

---

## 5. 组件规范

- **CTA 主按钮**：`--red` 底 + 白字，`border-radius:12~13px`，hover 变深 `#A80E26`，可带朱红柔光阴影。文案后跟 `→`（Space Mono）。
- **次按钮**：白底 + `--line` 描边，hover 描边变 `--ink`。
- **头像**：圆形，`border:3px solid #fff` + 柔和阴影，确保**全脸居中**入框。
- **数据/成绩卡**：墨黑标题条 + 白底网格，分数用 Sora 800。

---

## 6. 文案 Voice

- 中文为主，英文做点缀（eyebrow、英文副标、Space Mono 元信息）。
- 简洁、有力、可信；强调"提分 / 定制 / 名师 / 真题 / 一次出分"。
- 品牌 slogan：**学雅思 · 来雅速** / `STUDY IELTS · COME TO YASU`。
- 不堆砌 emoji（品牌不使用 emoji）。不编造数据；真实成绩/师资信息为准。

---

## 7. 禁止清单 Anti-patterns

- ❌ 大面积渐变 / 霓虹 / 玻璃拟态
- ❌ 纯白 `#fff` 大背景（用 `--paper`）
- ❌ 第二强调色
- ❌ Inter / Roboto / Arial
- ❌ emoji 装饰
- ❌ 用 margin / 空格排版（改用 flex/grid gap）
- ❌ 圆角气泡过度、阴影过重
- ❌ 编造师资履历或学员数据
